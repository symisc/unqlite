CMAKE_MINIMUM_REQUIRED(VERSION 3.0.0)
PROJECT(unqlite)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_LIST_DIR}/src)

SET(HEADERS_UNQLITE
  src/unqlite.h
)

SET(SOURCES_UNQLITE
  src/unqlite.c
)

ADD_LIBRARY(unqlite STATIC ${HEADERS_UNQLITE} ${SOURCES_UNQLITE})
